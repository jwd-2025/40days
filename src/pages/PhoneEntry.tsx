// This file is no longer used - replaced by FrontDoor.tsx (the front door
// now looks people up by email instead of phone number, since phone was
// never actually used for anything - see supabase/migrations/0006_drop_phone.sql).
// Nothing imports this file anymore. It's safe (and recommended) to delete
// it from your checkout:
//
//   git rm src/pages/PhoneEntry.tsx
//
// It's left here as an empty stub only because the sandbox this app was
// built in couldn't delete files, not because it's still needed.
export {}
