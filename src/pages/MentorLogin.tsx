// This file is no longer used - self-serve account creation was removed.
// Mentor/admin accounts are now only ever created by an admin, from
// MentorDashboard.tsx's "+ Add a mentor or admin" button (or the fuller
// form on AdminDashboard.tsx), never by the person themselves. Nothing
// imports this file or routes to it anymore. Safe to delete:
//
//   git rm src/pages/MentorLogin.tsx
//
// Left here as an empty stub only because the sandbox this app was built
// in couldn't delete files, not because it's still needed.
export {}
